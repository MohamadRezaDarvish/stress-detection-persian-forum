# Upstream resources

The original positive/negative lexicons and raw-post extractor were not supplied in the accepted handoff. The reproducible project starts from `member1_handoff.csv.gz`, where all required predefined count/signal columns already exist. For production raw-text ingestion, place the original extractor and resources here and preserve the exact columns documented in `data/processed/member1_preprocessor_config.json`.
